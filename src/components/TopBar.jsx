import React from "react";
import { AppBar, Typography, } from "@mui/material";

export default function TopBar(props) {
  return (
    <AppBar position="relative">
      <Typography>HEYYYYYYYYYYY</Typography>
    </AppBar>
  );
}
